//cuantas filas y columnas tiene una matriz
#include <stdio.h>
int main (){
    int j,i;
    int dimencion;
    scanf("%d \n%d %d", &dimencion, i, j);
    scanf("%d", &dimencion);
    printf ("ingrese una matriz");
    dimencion[i][j];
}